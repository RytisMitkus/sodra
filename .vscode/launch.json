{
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Run Script: serve",
            "request": "launch",
            "command": "yarn run serve",
            "cwd": "${workspaceFolder}"
        }
    ]
}