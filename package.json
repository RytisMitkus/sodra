{
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "deploy": "lerna run deploy",
        "serve": "lerna run serve --stream"
    },
    "devDependencies": {
        "lerna": "^4.0.0"
    },
    "name": "lerna-workspaces-private"
}
